---
###############################################################################
# Start
###############################################################################
# Start services and confirm they are active and running.

- name: 'Start | systemd | seerr.service'
  ansible.builtin.systemd:
    name: 'seerr.service'
    state: 'started'
    enabled: true
    daemon_reload: true
  delay: 15
  retries: 3
  register: seerr__service
  until: seerr__service.status.ActiveState == 'active'
  ignore_errors: true

- name: 'Start | fact | query services'
  ansible.builtin.service_facts:

- name: 'Start | assert | services running'
  ansible.builtin.assert:
    quiet: true
    that: ansible_facts.services[item].state == 'running'
    fail_msg: |

      ✘ Service failed to start.
      {{ item }}
  loop:
    - 'seerr.service'
